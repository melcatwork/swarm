{
  "build_command": "npm ci --prefix frontend && npm run build --prefix frontend && mkdir -p backend/static && cp -r frontend/dist/* backend/static/",
  "install_command": "cd backend && pip install -r requirements.txt",
  "start_command": "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
}
